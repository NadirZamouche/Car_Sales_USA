CREATE TABLE car_prices (
    year INTEGER,
    make VARCHAR,
    model VARCHAR,
    trim VARCHAR,
    body VARCHAR,
    transmission VARCHAR,
    vin VARCHAR,
    state VARCHAR,
    condition INTEGER,
    odometer INTEGER,
    color VARCHAR,
    interior VARCHAR,
    seller VARCHAR,
    mmr INTEGER,
    sellingprice INTEGER,
    saledate VARCHAR
);